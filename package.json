{
  "name": "@josyalem-studio/nestjsx-auth0",
  "version": "0.0.1",
  "description": "A nestjs implementation of auth0.",
  "author": "Yoseph Alem",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build",
    "pub": "npm publish --access public"
  },
  "dependencies": {
    "@nestjs/common": "^8.2.5",
    "rxjs": "^7.5.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
